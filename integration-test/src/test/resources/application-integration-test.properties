# Integration test configuration
# These values are overridden by environment variables in CI

api.base.url=${API_BASE_URL:http://localhost:8081}
yaci.store.url=${YACI_STORE_URL:http://localhost:8080/api/v1/}
