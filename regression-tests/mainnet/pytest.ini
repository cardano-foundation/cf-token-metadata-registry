[pytest]
testpaths = .
markers =
    cip26: CIP-26 off-chain token metadata tests
    cip68: CIP-68 on-chain token metadata tests
    v1: V1 API endpoint tests
    v2: V2 API endpoint tests
    batch: Batch query tests
    sanity: Sanity / smoke tests
