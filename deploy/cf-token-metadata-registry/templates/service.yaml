apiVersion: v1
kind: Service
metadata:
  name: {{ include "cf-token-metadata-registry.fullname" . }}
  labels:
    {{- include "cf-token-metadata-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cf-token-metadata-registry.selectorLabels" . | nindent 4 }}
